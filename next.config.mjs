/** @type {import('next').NextConfig} */
const nextConfig = {
	// output: 'export', // for static website
	// images: { unoptimized: true } // cant optimize images on a static webiste
	// reactStrictMode: false // disable default strict mode
};

export default nextConfig;
